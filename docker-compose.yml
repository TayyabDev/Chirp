version: "3"
services:
  api-server:
    # image: api-server
    build: ./backend
    ports:
      - "9080:9080"
    networks:
      - mern-app
  react-app:
    # image: react-app
    build: ./chirp
    stdin_open: true
    ports:
      - "80:80"
    networks:
      - mern-app
    # depends_on:
    #   - api-server
  media-server:
    image: media-server
    ports:
      - "1935:1935"
      - "8000:8000"
    networks:
      - mern-app
networks:
  mern-app:
    driver: bridge
# volumes:
#   mongo-data:
#     driver: local
